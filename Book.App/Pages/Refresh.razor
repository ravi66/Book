@page "/refresh"
@page "/refresh/{page}"
@inject NavigationManager NavigationManager

@code {
    [Parameter] public string Page { get; set; } = default!;

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"{Page}");
    }
}